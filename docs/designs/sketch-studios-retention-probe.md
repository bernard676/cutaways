# Design: Sketch Studios — Retention Probe

Generated by /office-hours on 2026-09-08
Branch: snpachat-removed-embedding
Repo: bernard676/cutaways (product name "Sketch Studios"; DB objects use the shared
`visualpedia_` prefix — all three names refer to the same thing)
Status: APPROVED
Mode: Startup

## Problem Statement

Sketch Studios is a working prototype: search any object, system, or concept and
get a generated technical cutaway illustration plus structured, explorable
knowledge (components, relationships, materials, construction steps, contextual AI
chat, recursive drill-down into sub-components). Mobile-first, Expo Go. Client-only
architecture — the app calls Claude (text/chat/vision) and Gemini (images) directly
and writes to Supabase Postgres/Storage under the signed-in user's RLS session. The
Home screen (verified in `src/app/(app)/index.tsx`) shows a recent-topics list (last
~10 viewed) and a "suggested topics" marquee that is a **hardcoded array of 6
labels** pre-filling a search. The semantic related-topics feature was removed with
the embeddings migration; this static surface is what remains, and it is the browse
loop the probe tests.

The founder wants to know whether this is a monetizable app. It reframed mid-session
from "a full startup" to "a cool app I can monetize" — this doc treats it as a
commercial indie app, not a venture company.

## Demand Evidence

Weak. Honest quotes from the session:

- "i would use it" — founder is the only user.
- "i never needed however i did use it last night, i was curious about a bridge" —
  the use case is curiosity, not need. Vitamin, not painkiller.
- "wikipedia is just plain text and would not actually explain, youtube it too long,
  it took 5 min" — the status quo genuinely underperforms for visual understanding.
- "i did come away understanding the bridge in a way Wikipedia wouldn't have gotten
  you to" — one real quality signal: the output beat the alternative for actual
  comprehension, in the founder's own use.
- "i have not showed this to anyone" — zero external exposure.

Net: no external demand evidence. One credible artifact-quality signal from n=1.

## Status Quo

When someone wants to understand how a thing works, the workflow they live with
today (founder's own words):

> Google → Wikipedia → skim → still confused → YouTube → scrub through a 20-min
> video → maybe a diagram in image search → give up or half-satisfied

Cost when unsolved: **$0.** The person shrugs and moves on. No exam failed, no
design shipped wrong, no deadline missed. This is the central constraint on
monetization.

## Target User & Narrowest Wedge

Not resolved. Founder named three groups and did not pick one:

- High-school / college STEM student — curiosity with a deadline (the exam).
- Science communicator / educator / textbook author — their output is the product.
- Hobbyist mid-build — consequence is breaking the expensive thing.

"A curious mind" is the founder's actual framing, and that is a category, not a
customer. Picking one is Gate 1 (see The Assignment).

Working wedge for the probe: one content domain — candidate "how everyday
mechanical things work" (bridges, engines, locks, pumps, HVAC) — not "all of human
knowledge."

## Constraints

- Expo Go today. Real distribution requires an EAS build. Expo Go is a dev harness,
  not a channel.
- `EXPO_PUBLIC_*` API keys (Anthropic, Gemini) are bundled into the client and
  extractable from the installed binary. Fine for a private prototype; a budget-drain
  vector the moment the app is distributed.
- No backend — Supabase is Postgres + Auth + Storage only. Any server code is
  net-new infrastructure.
- The app requires a signed-in Supabase session for its RLS writes. There is no
  anonymous auth today — a signup wall would sit in front of the first-open
  experience.
- Chat uses streaming responses from Claude. Anything that proxies chat must pass
  SSE through.
- Solo builder with no existing audience ("i have not showed this to anyone").
  Recruiting testers is real work, not a footnote.
- Search is keyword-only since the embeddings removal; semantic discovery is gone
  until a non-embeddings mechanism is added.

## Premises

All four confirmed by the founder ("all true"):

1. This is a curiosity / entertainment product, not a pain-killer utility.
   Monetization is engagement-driven (subscription for unlimited generations,
   one-time unlock, or ads), not "pro users pay to solve an urgent need."
2. The #1 unknown is **retention**, not quality. A curiosity app lives on the
   browse loop — a second and third session next week, unprompted. No monetization
   model survives zero retention. Test this before building more features.
3. The current architecture cannot be distributed as-is. Extractable keys mean
   someone drains the Anthropic/Gemini budget. Provider spend must be capped and
   the keys must not be trivially extractable before the app leaves the founder's
   own device.
4. The wedge is not "all knowledge." It is one domain where the cutaway is
   dramatically better than the alternatives and the audience over-indexes on
   curiosity. Ship narrow, expand from strength.

## Approaches Considered

### Approach A: Retention Probe (CHOSEN)
Ship the **existing app** to real external testers with analytics and the minimum
key/cost protection, and measure unprompted return over two weeks. The loop under
test is the current Home: a recent-topics list plus the 6 static suggested prompts.
The only browse-surface change allowed in the probe is swapping the hardcoded
`SUGGESTED_TOPICS` array for ~12 curated titles drawn from the seeded domain set (a
one-line constant change, not a feature). If this thin loop does not retain, that is
the finding, and Approach B is where a purpose-built feed gets designed. Effort M,
risk low.

### Approach B: Productized Curiosity App (deferred)
Full commit: real key-proxy backend, curated library of 100-200 pre-generated
cutaways, a purpose-built swipeable feed home, freemium (N free/day, subscription
for unlimited + save), both stores with ASO, camera scan as the "point at anything"
hook. Effort XL, risk med. Deferred: bets weeks of work on premise 2 before it is
validated. The feed, the real proxy, and CI/CD automation all live here. Revisit the
moment the probe shows a return curve.

### Approach C: Web-First Explain Engine (deferred)
Reframe to "visual explanation pages the web indexes" — shareable/embeddable "how
does X work" cutaway pages built for SEO, app becomes one client, monetize via
traffic then premium. Effort L, risk med-high. Deferred: abandons the built mobile
app for a 6+ month SEO payoff and a different tech stack. Revisit as a growth
channel if the core loop works.

## Recommended Approach

Approach A. It resolves premise 2 (retention) in ~2 weeks for the least work while
deferring every large build (feed, real proxy, monetization, CI/CD) to a point where
they are informed by a real return curve. Premise 4 (one domain) is honored by
seeding the probe with a single content area. If retention shows up, B and C both
remain open and better-informed.

## Probe Scope — What Actually Gets Built

### 1. Key / cost protection (probe-grade, not the full proxy)
For a ≤50-person closed beta the full backend proxy is over-build. Do the cheaper
mitigation and defer the real proxy to Approach B:
- Set hard monthly spend caps in both the Anthropic and Google AI consoles.
- Issue a **separate, restricted API key** for the beta build (not the founder's
  personal key). Assume it can be extracted; the spend cap is the real backstop.
- Add a remote kill switch: a `visualpedia_config` row (or Supabase Storage flag)
  the app reads on launch; when set, the app disables generation and shows a
  message. Lets the founder stop the bleeding within minutes without a store update.
  Needs a public / anon `SELECT` RLS policy on that row (or a public-read Storage
  object) so the anonymous session can read it.
- Log every generation's provider + estimated token/image cost to the analytics
  events (below) so spend is visible daily.
Full server-side proxy (JWT verification, per-user server-side rate limits, SSE
passthrough for chat, hosting decision) is written and built in Approach B.

### 2. Anonymous auth
Enable Supabase anonymous sign-in so the first open lands on the Home browse
surface with no signup wall. Upgrade-to-email is optional and offered later (e.g.
on bookmark or on the settings screen). This is a prerequisite for the
"usable within 5 seconds" criterion and for retention itself.

### 3. Analytics
PostHog (free tier) — it has cohort/retention tooling out of the box, which a
hand-rolled `visualpedia_events` table does not. Events, minimum set:
- `app_open` (props: `cold_start` bool, `days_since_install` int computed client-side
  from a stored install timestamp)
- `search_run` (props: `query`, `domain`)
- `topic_view` (props: `topic_id`, `source`: search | recent | suggested | drilldown)
- `component_drilldown`
- `chat_message_sent`
- `generation_completed` (props: `provider`, `est_cost_usd`)
- `bookmark_added`
Identify by the anonymous Supabase user id so cohorts line up with DB rows.
Add the App Store privacy nutrition label + ATT prompt for the SDK before submission.

**Measurement limitation:** anonymous auth mints a fresh id on reinstall or cleared
storage, which resets that tester's day 0 and silently deflates retention. Persist
the id and the install timestamp in the most durable client storage available
(`expo-secure-store`, which survives app reinstall on iOS), and treat the measured
curve as a **lower bound**, not an exact figure.

### 4. Content seed
Pre-generate 40-60 topics in the chosen domain so the app is not empty on first
open (recent + suggested both need rows). One-time script run by the founder;
budget the Gemini + Claude cost for ~60 generations (est. low tens of dollars —
confirm against current pricing before running).

### 5. Distribution
- EAS Build for iOS + Android.
- Google Play internal testing track (fast, no review friction).
- iOS TestFlight external testing — **requires Beta App Review** (allow 2-3 days,
  can be rejected). Start this early; if it slips, run the iOS cohort on internal
  TestFlight (up to 100 users by Apple ID) instead.

## Retention Metric — Operationalized

- **Definition:** classic day-N retention. A tester "installed on day 0" (first
  `app_open`). They are "retained on day N" if they have an `app_open` on calendar
  day N relative to their own day 0.
- **"Unprompted":** the app sends no push notifications and no reminder emails
  during the probe, so every open is unprompted by construction. State this so the
  number is not later misread.
- **Enrollment window:** recruit the whole cohort within a **3-day window** so the
  day-14 read is available for nearly everyone by the gate.
- **Primary gate (day 7):** measured at enrollment-window-end + 7 days.
- **Secondary read (day 14):** measured at enrollment-window-end + 14 days.
- **Bands (pre-committed), day-7 retention — judged on the 95% lower bound, not the
  point estimate,** because at n=20-50 the CI is roughly ±15pp and an XL commitment
  should not ride on noise:
  - **95% lower bound ≥ 20%** → clear pass. Proceed to Approach B. (At n≈30 that
    needs an observed rate near 37%+.)
  - **Observed ≤ 10%** → clear fail. The loop is the problem; do not add
    monetization. Fold in The Assignment learnings and reconsider the wedge or the
    whole direction.
  - **Anything between** → ambiguous. Run one two-week extension with a second
    recruited cohort, then decide on the pooled number (larger n tightens the
    bound). If still ambiguous after the pooled read, treat as a soft fail — the
    loop is not strong enough to build on yet.

## Open Questions

- Which single content domain? ("How everyday mechanical things work" is the
  working candidate — Gate 1 confirms or replaces it.)
- Which specific person inside that domain is the target? (Q3 was not answered.)
- Does the founder accept the 30% / 10% bands, or set different numbers before
  launch?
- Recruitment channels and who runs them (see Dependencies).
- `gemini-3-pro-image-preview` is a preview model — check batch rate/availability
  limits before the 60-topic seed run.

## Success Criteria

- Beta build ships with a restricted, spend-capped provider key and a working
  remote kill switch; the founder's personal key is not in the bundle.
- Anonymous sign-in works; first open reaches the Home browse surface in under 5
  seconds with no signup wall and no generation wait.
- One domain seeded with 40-60 pre-generated topics.
- PostHog wired with the event set above; retention dashboard shows a cohort curve.
- App Store privacy labels + ATT handled; build accepted to TestFlight (external or
  internal) and Play internal testing.
- 20-50 real external testers enrolled within a 3-day window (not friends-and-family
  only).
- Day-7 primary gate evaluated against the pre-committed bands; day-14 secondary
  read recorded.

## Distribution Plan

- Build: EAS Build (iOS + Android).
- Channel: Play internal testing + TestFlight (external if Beta App Review clears in
  time, internal otherwise). No public store listing.
- CI/CD: manual EAS builds for the probe. Automation deferred to Approach B.

## Dependencies (in gate order)

- **Gate 1 — The Assignment.** Five stranger conversations in the chosen domain
  before any probe code. Outcomes:
  - Strangers search, drill in, react positively → build the probe as scoped.
  - Strangers shrug / bounce → do not build the probe. The wedge or the direction
    is wrong; go back to picking a user.
- Then, in order:
  1. Domain decision (blocks the content seed).
  2. Anonymous auth + restricted key + kill switch + spend caps (blocks any
     external distribution).
  3. PostHog instrumentation (blocks the first install — no instrumentation, no
     data).
  4. Content seed (blocks a non-empty first open).
  5. EAS build + store tracks (blocks tester enrollment).
  6. Recruitment (blocks the enrollment window). Treat as a first-class work item,
     not a hope: **build a waitlist during Gate 1** — collect email signups from the
     same forum/Discord posts used for the stranger conversations, targeting ~15
     signups/day so a 20-50 cohort exists before the 3-day window opens. Named
     fallback if organic stalls after 2 days: paid recruiting (usertesting.com or a
     paid subreddit promo). The 3-day window opens only once the waitlist can fill
     it.
- Accounts: Apple Developer ($99/yr) + Google Play Console ($25 one-time).

## The Assignment

Before writing any probe code: **pick one domain, then get five real conversations
with strangers in it this week.** Not friends. Two workable methods, pick one:

1. **Moderated (higher signal):** post in 2-3 places the target hangs out
   (r/AskEngineers, r/MechanicalEngineering, a domain Discord, a hobbyist forum),
   offer a 15-minute screen-share call, and watch them use a build (Expo Go link,
   or a TestFlight/internal build if ready). Stay silent while they search one
   thing they have genuinely wondered about. Note: what they searched, whether they
   drilled in, whether they would open it again.
2. **Async (lower friction):** DM the same people a 60-second screen recording of
   the app, ask them to reply with (a) one thing they would search, (b) would they
   install it, (c) when did they last hit this problem for real.

Five conversations. That result is Gate 1 and it decides whether the probe gets
built at all.

## What I noticed about how you think

- You said "i never needed however i did use it last night" — you volunteered the
  weakest part of your own case without being cornered into it. That honesty is
  worth more than a polished pitch; it means the diagnosis can actually be trusted.
- You noticed "wikipedia is just plain text and would not actually explain" — that
  is a specific, felt observation about why the status quo fails, not a generic
  complaint. The taste that produced the cutaway output is the same taste that
  noticed this gap.
- You reframed mid-session from "full startup" to "a cool app that i can monetize."
  That is a real decision, not a retreat — it changes the bar you are held to, and
  you made it without prompting.
- You pushed on process ("do not ask redundant questions… you have access to the
  full source code") but agreed with all four premises without a fight. The pushback
  is on my efficiency, not on the substance — which means the substance landed.
